# Modules 2.0 follow-ups

The core Modules 2.0 implementation is complete. Close the remaining work one item at a time:

- [ ] Complete the Mod Store detail view: README, configuration preview, dependencies, and author link.
- [ ] Implement runtime support for translation-pack mods.
- [ ] Add avatar editing that does not require `sonder@core-identity`.
- [ ] Validate mod manifests against the registry schema during builds and app-side installation.
- [ ] Add dev-mode live-reload e2e coverage and an e2e fixture-drift CI check.
- [ ] Complete manual web, iOS, and Android verification for Mod Store installation, external dice, and themes.
- [ ] Clean up remaining author documentation, including the theme CSS variable contract and registry mirror guidance.

Remove each item when its implementation and relevant verification are complete.
